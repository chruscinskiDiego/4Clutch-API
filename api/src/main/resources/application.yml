server:
  port: 8025
spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    generate-unique-name: false
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    properties:
      javax:
        persistence:
          validation:
            mode: none
